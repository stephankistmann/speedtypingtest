import styled from "styled-components";

export const CustomInput = styled.input`
  height: 1.5rem;
  width: 7rem;
  margin-right: 0.5rem;
  color: var(--background);
  &:focus {
    outline: none;
  }
  font-weight: 600;
`;
